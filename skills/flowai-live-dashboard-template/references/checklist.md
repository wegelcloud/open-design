# Checklist

## P0

- `assets/template.html` exists and opens from disk.
- `example.html` is a complete, hand-built sample with real labels and values.
- Skill is `od.mode: template` and `od.scenario: live-artifacts`.
- No external avatar/photo CDN dependencies in template sample.
- Tabs switch views correctly.
- Role bar chart animates with easing on details tab enter.
- Chart hover tooltips show precise label/value.
- Modules can zoom in/out via click.
- Dark mode toggle works.

## P1

- CSV export works for team rows and workflow rows.
- Mobile fallback stacks columns under 1300px.
- Colors and spacing inherit from root tokens.

## P2

- Tooltip avoids viewport edge clipping.
- Chart values animate smoothly on re-render.
